language: node_js

os: linux

node_js:
  - node

branches:
  only:
    - master

script:
  - npm test
  - npm run-script build

deploy:
  provider: releases
  token: ${MY_TRAVIS_TOKEN}
  file_glob: true
  file: dist/*
  cleanup: true
  on:
    tags: true