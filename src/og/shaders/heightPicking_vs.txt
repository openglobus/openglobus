precision highp float;

attribute vec3 aVertexPosition;

uniform mat4 projectionViewMatrix;
uniform vec3 camPos;

varying float range;

void main(void) {
	range = distance(camPos, aVertexPosition);
    gl_Position = projectionViewMatrix * vec4(aVertexPosition, 1.0);
}