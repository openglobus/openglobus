/** Planet terrain + geoObject: G-buffer / forward (same names as in shaders/common/shadeMode.glsl). */
export const SHADE_MODE_UNLIT = 0;
export const SHADE_MODE_PHONG = 1;
export const SHADE_MODE_PBR = 2;
