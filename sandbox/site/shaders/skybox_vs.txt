attribute vec3 aVertexPosition;

uniform mat4 uPMVMatrix;
uniform vec3 pos;

varying vec3 vTextureCoord;

void main(void) {
    vTextureCoord = aVertexPosition;
    gl_Position = uPMVMatrix * vec4(aVertexPosition + pos, 1.0);
}